const Snap = require('../lib');



// internals.snapshot({volumeTag: 'lambda-snapshot'}, (error, result) => {
// internals.findSnapshotsWithTag('lambda-snapshot', (error, result) => {
// internals.keep('lambda-snapshot', 2, (error, result) => {
//   if(error){
//     return console.log('error: ' + error);
//   }
//   console.log(JSON.stringify(result));
// });
